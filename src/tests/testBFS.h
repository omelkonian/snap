/*
 * testBFS.h
 *
 *  Created on: Nov 14, 2014
 *      Author: Orestis Melkonian
 */

#pragma once
#include <iostream>

#include "general.h"
#include "../search/BFS.h"

using namespace std;

void _testBFS() {
	// Create BucketList
//	BucketList *bucketList = createBucketList(25, 4);
//	bucketList->printIDs();

	/*
	 BFS *bfs = new BFS(bucketList, 1, 6);

	 bfs->printAlreadyVisited();
	 bfs->step();
	 bfs->printAlreadyVisited();
	 bfs->step();
	 bfs->printAlreadyVisited();
	 bfs->step();
	 bfs->printAlreadyVisited();
	 bfs->step();
	 bfs->printAlreadyVisited();
	 bfs->step();
	 bfs->printAlreadyVisited();
	 bfs->step();
	 bfs->printAlreadyVisited();

	 cout << "THE END!!!" << endl;
	 */
}
